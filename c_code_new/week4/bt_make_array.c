// Zur Erinnerung nur die Aufgabe, keine Lösung: eine Funktion
// die mit malloc ein Array anlegt, es füllt (z.B. i * 10) und den Pointer zurückgibt.
// Im main fängst du ihn auf, gibst das Array aus, und gibst es mit free frei.
